extends ../layout

block content
  .text-center
    h1= title
    
    h2 Are you sure you want to delete the book, "#{book.title}"?
    form.delete-form(action=`/books/${book.id}/delete`, method="post")
      a(href=`/books/${book.id}`) No! Take me back! 
      | 
      input(type="submit", value = "Yes, I'm sure")